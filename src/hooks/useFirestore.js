//add new document or remove document
import { useReducer, useEffect, useState } from "react"
import { projectFirestore } from "../firebase/config"


export const useFirestore = (collection) => {
    
    return {}
}